services:
  - type: web
    name: moviemobs-angular
    env: static
    buildCommand: "npm install && npm run build"
    staticPublishPath: dist/MovieMobsAngular
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
